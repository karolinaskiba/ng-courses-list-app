import { HightlightedDirective } from './hightlighted.directive';

describe('HightlightedDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightedDirective();
    expect(directive).toBeTruthy();
  });
});
