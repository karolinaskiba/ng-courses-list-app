import { StrukturalDirective } from './struktural.directive';

describe('StrukturalDirective', () => {
  it('should create an instance', () => {
    const directive = new StrukturalDirective();
    expect(directive).toBeTruthy();
  });
});
